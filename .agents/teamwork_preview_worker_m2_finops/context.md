# Context for Specialist Worker: finops-financas.html

## Target File
`/pages/financas/finops-financas.html`

## Requirements
1. Detail FinOps cost optimization applied to financial data processing (Apache Iceberg Vacuum, Compaction, S3 Lifecycle Tiers).
2. Include clean SVG diagrams and simulated cost comparison tables (before vs. after FinOps optimization).
3. Ensure zero KaTeX syntax inside SVG text nodes, and use `\(` / `\[` for math formulas in HTML.
4. Dark mode premium design (`style.css`).
